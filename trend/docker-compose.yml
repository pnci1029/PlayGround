services:
  # Trend 백엔드 서비스 (Node.js + TypeScript)
  trend:
    build:
      context: ./trend_be
      dockerfile: Dockerfile
    image: trend-backend:latest
    container_name: trend
    ports:
      - "8002:8002"  # HTTP API
      - "8012:8012"  # WebSocket
    env_file:
      - /root/playground/.env
    # migrations 폴더를 런타임에 제공 (Dockerfile COPY 대신). trend.sql 자동 마이그레이션용.
    volumes:
      - /root/playground/migrations:/app/migrations:ro
    environment:
      - NODE_ENV=production
      - PORT=8002
      - WS_PORT=8012
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${POSTGRES_DB:-playground}
      - DB_USER=${POSTGRES_USER:-postgres}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_SCHEMA=trend
    restart: unless-stopped
    networks:
      - playground_network

networks:
  playground_network:
    external: true
    name: playground_playground_network