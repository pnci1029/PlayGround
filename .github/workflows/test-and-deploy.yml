name: Test and Auto Deploy

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: |
          blog/blog_fe/package-lock.json
          blog/blog_be/package-lock.json
          
    - name: Install dependencies and test builds
      run: |
        # 블로그 프론트엔드 테스트
        cd blog/blog_fe
        npm ci
        npm run build
        cd ../..
        
        # 블로그 백엔드 테스트  
        cd blog/blog_be
        npm ci
        npm run type-check
        cd ../..
        
        echo "✅ 모든 빌드 테스트 통과"

  deploy:
    needs: test
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # 🔑 .gitignore된 실제 파일들을 복원
    - name: Restore gitignored files
      run: |
        # (제거됨) playground/be/.env — 컨테이너는 루트 /root/playground/.env 만 읽으므로 개별 .env 복원 불필요

        # (제거됨) playground/fe/* — 프론트엔드는 Vercel 배포라 Contabo 배포엔 불필요

        # 공통 .env.production 복원 (최상위)
        if [ ! -z '${{ secrets.ENV_PRODUCTION_FILE }}' ]; then
          echo '${{ secrets.ENV_PRODUCTION_FILE }}' | base64 -d > .env.production
        fi
        
        # 마이그레이션 파일들 복원
        mkdir -p migrations
        if [ ! -z '${{ secrets.MIGRATION_SERVICE_FILE }}' ]; then
          echo '${{ secrets.MIGRATION_SERVICE_FILE }}' | base64 -d > migrations/migration.service.ts
        fi
        if [ ! -z '${{ secrets.MIGRATION_PLAYGROUND_SQL }}' ]; then
          echo '${{ secrets.MIGRATION_PLAYGROUND_SQL }}' | base64 -d > migrations/playground.sql
        fi
        
        # (제거됨) playground/fe/src/lib/config.ts — 프론트는 Vercel + git 추적 코드라 시크릿 복원 불필요

        # 메인 .env 복원
        if [ ! -z '${{ secrets.MAIN_ENV_FILE }}' ]; then
          echo '${{ secrets.MAIN_ENV_FILE }}' | base64 -d > .env
        fi
        
        # MoodBite BE secret 복원
        if [ ! -z '${{ secrets.MOODBITE_BE_SECRET_FILE }}' ]; then
          mkdir -p moodbite/MoodBite_BE/src/main/resources
          echo '${{ secrets.MOODBITE_BE_SECRET_FILE }}' | base64 -d > moodbite/MoodBite_BE/src/main/resources/application-secret.yml
        fi
        
        # MoodBite foods.json data 복원 (압축됨)
        if [ ! -z '${{ secrets.MOODBITE_FOODS_JSON_FILE }}' ]; then
          mkdir -p moodbite/MoodBite_BE/src/main/resources/data
          echo '${{ secrets.MOODBITE_FOODS_JSON_FILE }}' | base64 -d | gunzip > moodbite/MoodBite_BE/src/main/resources/data/foods.json
        fi
        
        # (제거됨) blog/blog_be/.env — 컨테이너는 루트 /root/playground/.env 만 읽으므로 개별 .env 복원 불필요

        # (제거됨) blog/blog_be/src/utils/config.ts — git 추적 코드. 시크릿이 git 코드를 덮어쓰던 잔재라 제거(이제 git이 소스)

    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        echo '${{ secrets.SSH_PRIVATE_KEY }}' | base64 -d > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H ${{ secrets.CONTABO_HOST }} >> ~/.ssh/known_hosts
        chmod 644 ~/.ssh/known_hosts

    - name: Package source code
      run: |
        # 백엔드 관련 파일들만 압축 (서버에서 Docker가 빌드)
        tar --exclude='node_modules' \
            --exclude='dist' \
            --exclude='.next' \
            --exclude='build' \
            --exclude='*.map' \
            --exclude='*.git*' \
            --exclude='.npm' \
            --exclude='.cache' \
            --exclude='playground/fe' \
            --exclude='moodbite/MoodBite_FE' \
            --exclude='trend/trend_fe' \
            --exclude='blog/blog_fe' \
            --exclude='story/story_fe' \
            -czf playground-deploy.tar.gz \
            playground/be/ \
            moodbite/MoodBite_BE/ \
            trend/trend_be/ \
            blog/blog_be/ \
            blog/docker-compose.yml \
            playground/docker-compose.yml \
            moodbite/docker-compose.yml \
            trend/docker-compose.yml \
            stock_screener/docker-compose.yml \
            stock_screener/stock_screener_be/ \
            story/docker-compose.yml \
            story/story_be/ \
            migrations/ \
            docker-compose.yml \
            docker-compose.db.yml \
            docker-compose.blog.yml \
            caddy/ \
            deploy.sh \
            .env \
            .env.production \
            $([ -d database ] && echo database/ || echo)

    - name: Detect service changes
      id: changes
      run: |
        echo "Detecting changes..."

        # 푸시 '전체 범위'를 비교한다.
        # (기존엔 HEAD~1..HEAD 로 마지막 커밋 1개만 봐서,
        #  여러 커밋 푸시 시 마지막이 아닌 커밋의 BE 변경을 놓쳤음)
        BEFORE='${{ github.event.before }}'
        AFTER='${{ github.sha }}'

        if [ -n "$BEFORE" ] \
           && [ "$BEFORE" != "0000000000000000000000000000000000000000" ] \
           && git rev-parse --quiet --verify "$BEFORE^{commit}" >/dev/null 2>&1; then
          echo "Using git diff $BEFORE..$AFTER (full push range)"
          CHANGED_FILES=$(git diff --name-only "$BEFORE" "$AFTER")
        else
          echo "before SHA unavailable (first push/force-push) — using last commit"
          CHANGED_FILES=$(git diff-tree --no-commit-id --name-only -r HEAD)
        fi

        echo "Changed files:"
        echo "$CHANGED_FILES"
        
        # 백엔드 서비스별 변경 여부 확인
        echo "playground_backend=$(echo "$CHANGED_FILES" | grep -q '^playground/be/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "moodbite_backend=$(echo "$CHANGED_FILES" | grep -q '^moodbite/MoodBite_BE/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "trend_backend=$(echo "$CHANGED_FILES" | grep -q '^trend/trend_be/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "blog_backend=$(echo "$CHANGED_FILES" | grep -q '^blog/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "stock_screener=$(echo "$CHANGED_FILES" | grep -q '^stock_screener/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "story_backend=$(echo "$CHANGED_FILES" | grep -qE '^story/(story_be/|docker-compose)' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "caddy_config=$(echo "$CHANGED_FILES" | grep -q '^caddy/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "docker_config=$(echo "$CHANGED_FILES" | grep -q '^docker-compose' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT

    - name: Deploy to Contabo
      run: |
        # 디렉토리 생성 후 배포 파일 업로드
        ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/id_rsa ${{ secrets.CONTABO_USER }}@${{ secrets.CONTABO_HOST }} \
          "mkdir -p ${{ secrets.CONTABO_PATH }}"
        
        scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/id_rsa playground-deploy.tar.gz \
          ${{ secrets.CONTABO_USER }}@${{ secrets.CONTABO_HOST }}:${{ secrets.CONTABO_PATH }}/
        
        # 서버에서 소스 배포 및 deploy.sh 실행
        # ⚠️ tar -xzf 는 압축파일에 없는 파일을 지우지 않는다(덮어쓰기/추가만).
        #    그래서 git에서 삭제한 소스가 서버에 영원히 남아 컴파일이 깨진다.
        #    압축 풀기 전에 소스 트리를 비워 '삭제'까지 반영한다.
        #    (application-secret.yml, foods.json 등 gitignore 파일은 tar 안에 포함되어 다시 복원됨)
        ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/id_rsa ${{ secrets.CONTABO_USER }}@${{ secrets.CONTABO_HOST }} \
          "cd ${{ secrets.CONTABO_PATH }} && \
           rm -rf playground/be moodbite/MoodBite_BE trend/trend_be blog/blog_be stock_screener/stock_screener_be story/story_be && \
           tar -xzf playground-deploy.tar.gz && \
           rm -f playground-deploy.tar.gz && \
           chmod +x deploy.sh && \
           BLOG_CHANGED='${{ steps.changes.outputs.blog_backend }}' \
           MOODBITE_CHANGED='${{ steps.changes.outputs.moodbite_backend }}' \
           PLAYGROUND_CHANGED='${{ steps.changes.outputs.playground_backend }}' \
           TREND_CHANGED='${{ steps.changes.outputs.trend_backend }}' \
           STOCK_SCREENER_CHANGED='${{ steps.changes.outputs.stock_screener }}' \
           STORY_CHANGED='${{ steps.changes.outputs.story_backend }}' \
           DOCKER_CHANGED='${{ steps.changes.outputs.docker_config }}' \
           CADDY_CHANGED='${{ steps.changes.outputs.caddy_config }}' \
           ./deploy.sh"

    - name: Post-deployment health check
      run: |
        echo "⏳ 배포 후 헬스체크 대기 중..."
        sleep 10
        
        # 블로그 API 헬스체크
        if ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/id_rsa ${{ secrets.CONTABO_USER }}@${{ secrets.CONTABO_HOST }} \
           "curl -s -f http://localhost:8003/health"; then
          echo "✅ 블로그 API 헬스체크 성공"
        else
          echo "❌ 블로그 API 헬스체크 실패"
          ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/id_rsa ${{ secrets.CONTABO_USER }}@${{ secrets.CONTABO_HOST }} \
            "docker logs blog --tail 10"
          exit 1
        fi
        
        # 외부 API 접근 테스트
        if curl -s -f https://blog-api.chhong.kr/health; then
          echo "✅ 외부 블로그 API 접근 성공"
        else
          echo "❌ 외부 블로그 API 접근 실패"
          exit 1
        fi
        
        echo "🎉 배포 및 헬스체크 완료!"